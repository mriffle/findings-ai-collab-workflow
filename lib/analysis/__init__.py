"""Analysis templates (lib/) — entry-point statistics seeds.

Imported as ``from analysis.<module> import ...`` (the repo pyproject puts ``lib/`` on
the path); a project adapts the prefix when it seeds them into its own modules.
"""
